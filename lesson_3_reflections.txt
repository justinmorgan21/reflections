When would you want to use a remote repository rather than keeping all your work local?

The remote repository can be used as a backup in case anything happens to your local machine, and it also allows for collaboration if it is hosted on a server with access by multiple users.
